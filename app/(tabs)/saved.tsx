import { Text, View } from 'react-native';

export default function SavedScreen() {
    return (
        <View className="flex-1 items-center justify-center bg-background">
            <Text className="text-text">Saved Posts</Text>
        </View>
    );
}
